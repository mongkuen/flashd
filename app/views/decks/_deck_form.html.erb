<%= bootstrap_form_for @deck do |f| %>
  <%= f.text_field :name, hide_label: true, placeholder: "Deck Name (Required)" %>
  <%= f.text_area :img_url, :rows => "2", hide_label: true, placeholder: "Background Image Url (Optional)" %>
  <%= f.submit @deck.new_record? ? "Create Deck" : "Edit Deck", class: "btn btn-primary" %>
  <%= link_to "Back", "javascript:history.back()", class: "btn btn-danger" %>
<% end %>
